// import solidity
pragma soldity >=0.8.2 <0.9.0;

//import 
// import "filename"; 
// import * as symbolname as "filename2";
// import {symbol1 as alias, symbol2} from "filename3";

// first contract
contract SimpleContract {
    // ether gwei wei
    bool isEqual = (1 ether == 1e18 wei);

    // time units : seconds, minutes, hours, days, weeks
    bool isTime = (1 hours == 60 minutes);

    // this variables can not be used variable

}

